# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# MET API
GET     /v0.:format                 controllers.ElementsController.getElements(ids: Option[String], legacyElemCodes: Option[String], lang: Option[String], format: String)
GET     /:id/v0.:format             controllers.ElementsController.getElementById(id: String, lang: Option[String], format: String)
